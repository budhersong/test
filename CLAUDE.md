# CLAUDE.md

## Repository Overview

This is the **test** repository (`budhersong/test`). It is currently a new project in its initial state.

## Project Structure

```
.
├── CLAUDE.md       # AI assistant guide (this file)
└── README.md       # Project readme
```

## Development Setup

No build tools, package managers, or dependencies are configured yet. When the project grows, update this section with:

- Language and runtime versions
- Install commands (e.g., `npm install`, `pip install -r requirements.txt`)
- Environment variable setup

## Common Commands

_No build, test, or lint commands are configured yet._ Update this section as tooling is added.

<!-- Example entries to fill in later:
```
npm install          # Install dependencies
npm run build        # Build the project
npm test             # Run tests
npm run lint         # Run linter
```
-->

## Code Style and Conventions

No linter or formatter configuration exists yet. When added, document:

- Linting tool and config file location
- Formatting tool and config file location
- Any project-specific style rules

## Testing

No test framework is configured yet. When added, document:

- Test framework and runner
- How to run the full test suite
- How to run a single test file
- Test file naming conventions and location

## Git Workflow

- **Default branch**: `master`
- Write clear, descriptive commit messages
- Keep commits focused on a single change

## Architecture Notes

_To be updated as the project develops._ Document key design decisions, module boundaries, and data flow here.
